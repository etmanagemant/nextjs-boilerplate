# NPM Configuration for Playwright on Vercel
# Since we use Browserless.io (remote browser), we DON'T need local browser binaries

# Set via env var on Vercel instead
# This file is optional - Vercel will set PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 automatically
